 @model EventDeleteViewModel

@{
    ViewBag.Title = "Delete Seminar";
}

<h2 class="text-center">@ViewBag.Title</h2>
<hr />

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card">
            <div class="card-body">
                <h3 class="card-title text-center">@Model.Name</h3>
                <img class="card-img-top" style="width: 20rem;"
                     src="@Model.ImageUrl" alt="House Image">
                <p class="text-danger mt-3">"Сигурен ли си, че искаш да изтриеш събитието"</p>
            </div>
            <div class="card-footer">
                <form asp-action="DeleteConfirmed" method="post">
                    <input type="hidden" asp-for="Id" />
                    <button type="submit" class="btn btn-danger">Confirm Delete</button>
                    <a asp-action="All" class="btn btn-secondary">Cancel</a>
                </form>
            </div>
        </div>
    </div>
</div>


